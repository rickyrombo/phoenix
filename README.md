# Phoenix

An open source and decentralized music distribution platform.

## Running

1. Set up an .env file in the root

2. Bring up services

```bash
docker compose up -d
```

3. Run the web app against them

```bash
cd web
npm run dev
```